@model List<DataLayer.Models.Produits>
@using Form114.Infrastructure
@{
    ViewBag.Title = "Result";
}

<h2>Result</h2>

@*<div class="row">*@
@*@foreach (var item in @Model)
    {
        <p>@item.IdProduit</p>
        <p>@item.Prix</p>
        <p>@item.Adresse</p>
    }*@

<table class="table table-hover table-striped table-bordered table-responsive">
    <thead>
        <tr>
            <th>Ville</th>
            <th>nombre de chambre</th>
            <th>adresse</th>
            <th>Prix</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {

            <tr>
                <td>@item.Villes.name</td>
                <td>@item.NbPlaces</td>
                <td>@item.Adresse</td>
                <td>@item.Prix.FirstOrDefault().Montant</td>
                <td><a href="#">Réserver</a></td>
            </tr>

        }
    </tbody>

</table>

@*@for (int i = 0; i < ViewBag.listeChoisi.Length; i++)
    {
        @ViewBag.listeChoisi[i]
    }*@
@*</div>*@
